# 🧮 Matrix Calculator (Python + NumPy)

A simple command-line matrix calculator written in Python using NumPy.  
It allows users to input two matrices and performs various operations like addition, subtraction, multiplication, dot product, transpose, determinant, and inverse.

---

## 🚀 Features

- Matrix input with validation
- Supports floating-point numbers
- Performs:
  - Addition
  - Subtraction
  - Element-wise multiplication
  - Dot product
  - Transpose
  - Determinant (for square matrices)
  - Inverse (for invertible square matrices)
- Error handling for dimension mismatches

---

## 🛠️ Requirements

- Python 3.7 or above  
- NumPy library

---

## 📦 Installation

Clone the repository:

```bash
git clone https://github.com/<your-username>/matrix-calculator.git
cd matrix-calculator
